# f(x) = x^T*w (b is hidden in x and w)
# y = f(x) + eps, eps ~ N(0, sigm_n^2)
# sigm_n - std of noise
# w ~ N(0, sigm_p)
# sigm_p - covariance matrix for params GP

import numpy as np
import matplotlib.pyplot as plt


def main():
    pass

if __name__ == '__main__':
    main()